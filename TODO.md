# TODO (Testing)

## Plan items
- [ ] Add Vitest + Playwright dependencies
- [ ] Add Vitest config + setup file
- [ ] Add Playwright config
- [ ] Add example smoke tests:
  - [ ] Unit test for `cn` utility
  - [ ] E2E smoke test for homepage load
- [ ] Wire scripts into `package.json` (`test`, `test:watch`, `test:e2e`)
- [ ] Add minimal CI command placeholders (document only if CI config doesn’t exist)

## Verification
- [ ] Run `npm test`
- [ ] Run `npm run test:e2e -- --headed=false` (headless default)
- [ ] Run `npm run lint` to ensure no lint breakages

